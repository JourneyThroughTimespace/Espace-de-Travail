using UnityEngine;
using System.Collections;

public class qddlight : MonoBehaviour {
    public GameObject light; 

	// Use this for initialization
	void Start () {
        Instantiate(light);	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
